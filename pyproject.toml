[build-system]
requires = ["setuptools>=43", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--doctest-modules --doctest-glob='README.rst'"

[tool.coverage.run]
source = ["src/django_datatables_serverside"]

[tool.coverage.html]
directory = "htmlcov"